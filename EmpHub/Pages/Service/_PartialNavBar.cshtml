@{
    var token = User.AccessToken(Context);
}
<div class="d-flex mb-1">
    <div class="flex-fill">
        <div class="d-flex align-items-end">
            <div class="me-2">
                <small>Service No</small>
                <div class="d-flex">
                    <input class="form-control form-control-sm" ng-model="serviceNo" my-enter="LoadData()" />
                </div>
            </div>
            <div class="me-2">
                <small>Category</small>
                <div class="d-flex">
                    <input class="form-control form-control-sm" ng-model="categoryDesc" my-enter="LoadData()" />
                </div>
            </div>
            <div class="me-2">
                <small>Request By</small>
                <div class="d-flex">
                    <input class="form-control form-control-sm" ng-model="createName" my-enter="LoadData()" />
                </div>
            </div>
            <div class="me-2">
                <small>Request Date</small>
                <div class="d-flex">
                    <input class="form-control form-control-sm icon-calendar date"
                           ng-model="createDate"
                           ng-model-options="{ updateon: 'blur' }"
                           moment-picker="createDate"
                           locale="th"
                           format="DD/MM/YYYY"
                           min-view="month"
                           max-view="month"
                           start-view="month"
                           my-enter="LoadData()" />
                </div>
            </div>
            <div class="me-2">
                <button ng-click="LoadData()" class="btn btn-circle" title="Search">
                    <i class="fas fa-search"></i>
                </button>
            </div>
            <div class="me-2">
                <button ng-click="Download()" class="btn btn-circle" title="Download">
                    <i class="fas fa-arrow-down"></i>
                </button>
            </div>
            <div class="flex-fill">

            </div>
        </div>
    </div>
    <div class="text-end d-flex flex-nowrap align-items-center">
        <span class="d-none d-md-block me-2">
            {{ (total > 0 ? ((page - 1) * row) + 1 : 0) | number }} - {{ (page * row < total ? page * row : total) | number }} of {{ total | number }}
        </span>
        <button class="btn btn-circle me-1" ng-click="SetPage(page - 1)" ng-disabled="page <= 1" title="Previous Page">
            <i class="fas fa-chevron-left"></i>
        </button>
        <button class="btn btn-circle" ng-click="SetPage(page + 1)" ng-disabled="page * row >= total" title="Next Page">
            <i class="fas fa-chevron-right"></i>
        </button>
    </div>
</div>