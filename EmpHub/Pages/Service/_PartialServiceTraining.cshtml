@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    var token = User.AccessToken(Context);
}
<div ng-controller="ServiceTrainingCtrl" class="mb-4 text-dark">
    <div class="list-group-item px-0 d-flex" ng-repeat="x in lData">
        <div style="width:20px" class="me-2 fw-bolder">
            #{{ $index + 1 }}.
        </div>
        <div class="flex-fill">
            <div class="row">
                <div class="col-sm-8 mb-3">
                    <small class="text-decoration-underline">
                        ชื่อใบอนุญาต
                    </small>
                    <div class="fw-bolder">{{ x.license != '' ? x.license : '-' }}</div>
                </div>
                <div class="col-sm-2 mb-3">
                    <small class="text-decoration-underline">
                        วันที่ออกเอกสาร
                    </small>
                    <div class="fw-bolder">{{ x.issueDate != '' ? x.issueDate : '-' }}</div>
                </div>
                <div class="col-sm-2 mb-3">
                    <small class="text-decoration-underline">
                        วันหมดอายุ
                    </small>
                    <div class="fw-bolder">{{ x.expireDate != '' ? x.expireDate : '-' }}</div>
                </div>
                <div class="col-sm-12 mb-3">
                    <small class="text-decoration-underline">
                        องค์กรที่ออกเอกสาร
                    </small>
                    <div class="fw-bolder">{{ x.organization != '' ? x.organization : '-' }}</div>
                </div>
                <div class="col-sm-12 mb-3">
                    <small class="text-decoration-underline">
                        รายละเอียด
                    </small>
                    <div class="fw-bolder">{{ x.description != '' ? x.description : '-' }}</div>
                </div>
            </div>
        </div>
    </div>
</div>
<script>
    app.controller('ServiceTrainingCtrl', function ($scope, $http, $timeout, $filter) {

        $scope.LoadData = function () {
            $.ajax({
                url: '@webAPI.CurrentValue.APIEmpHub/api/ServiceTraining/Detail',
                headers: { 'authorization': 'Bearer @token' },
                type: "POST",
                contentType: 'application/json',
                dataType: 'json',
                cache: false,
                data: JSON.stringify({
                    refId: $scope.refId
                }),
                success: function (result) {
                    $timeout(async function () {
                        $scope.lData = result;
                    }, 0);
                }, error: AjaxError
            });
        }

        $scope.$parent.LoadTraining = function (x) {
            $scope.refId = x.id;
            $scope.userId = x.userId;
            $scope.status = x.status;
            $scope.LoadData();
        }
    });
</script>