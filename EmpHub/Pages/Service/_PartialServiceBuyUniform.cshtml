@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    var token = User.AccessToken(Context);
}
<div ng-controller="ServiceBuyUniformCtrl">
    <div class="bg-white rounded-4 border mb-4 overflow-hidden">
        <div class="px-4 py-3 border-bottom d-flex align-items-center">
            <div class="fs-5 fw-bolder text-dark flex-fill">Request Buy Uniform</div>
        </div>
        <div class="p-4">
            <table class="table table-borderless">
                <tbody>
                    <tr>
                        <td width="200">
                            <div class="text-dark text-nowrap fw-bolder">แบบ</div>
                            <div class="text-dark text-nowrap">Style</div>
                        </td>
                        <td class="text-blue">
                            {{ iForm.style }}
                        </td>
                    </tr>
                    <tr>
                        <td width="200">
                            <div class="text-dark text-nowrap fw-bolder">จำนวน</div>
                            <div class="text-dark text-nowrap">Quantity</div>
                        </td>
                        <td class="text-blue">
                            {{ iForm.quantity | number }}
                        </td>
                    </tr>
                    <tr>
                        <td>
                            <div class="text-dark text-nowrap fw-bolder">หมายเหตุ</div>
                            <div class="text-dark text-nowrap">description</div>
                        </td>
                        <td class="text-blue">
                            {{ iForm.description }}
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>
<script>
    app.controller('ServiceBuyUniformCtrl', function ($scope, $http, $timeout, $filter) {
        $scope.LoadDetail = function () {
            return new Promise((resolve) => {
                $.ajax({
                    url: '@webAPI.CurrentValue.APIEmpHub/api/Form/FormBuyUniformDetail',
                    headers: { 'authorization': 'Bearer @token' },
                    type: "POST",
                    contentType: 'application/json',
                    dataType: 'json',
                    cache: false,
                    data: JSON.stringify({
                        refId: $scope.refId
                    }),
                    success: function (result) {
                        $timeout(async function () {
                            $scope.iForm = result;
                            resolve(true);
                        }, 0);
                    }, error: function () {
                        resolve(false);
                    }
                });
            });
        }

        $scope.$parent.LoadFormBuyUniform = async function (x) {
            $scope.refId = x.id;
            $scope.userId = x.userId;
            $scope.status = x.status;

            await $scope.LoadDetail();
        }
    });
</script>