@page
@model EmpHub.Pages.Onboarding.UpdateModel
@{
    ViewData["Title"] = "Onboarding";
    var token = User.AccessToken(HttpContext);
}
@section PageHeader {
    <!-- [ page-header ] start -->
    <div class="page-header">
        <div class="page-header-left d-flex align-items-center">
            <div class="page-header-title">
                <h5 class="m-b-10">Onboarding</h5>
            </div>
            <ul class="breadcrumb">
                <li class="breadcrumb-item"><a href="@Url.Page("/Index")">Home</a></li>
                <li class="breadcrumb-item"><a href="@Url.Page("Index")">Onboarding</a></li>
                <li class="breadcrumb-item">Update</li>
            </ul>
        </div>
    </div>
}
<div ng-controller="ManageCtrl" class="p-2">
    <partial name="_PartialOnboard" />
</div>
<script>
    app.controller('ManageCtrl', function ($scope, $http, $timeout, $filter) {
        $scope.page = 1
        $scope.row = 10;
        $scope.step_index = 0;
        $scope.userId = '@Model.userId';

        $scope.lSex = [
            { key: 'ชาย', value: 'M' }
            , { key: 'หญิง', value: 'F' }
        ];
        $scope.lProbationDay = [90, 120, 180];

        $scope.iPersonal = {};
        $scope.iEmployee = {};
        $scope.iAddress = {};

        $scope.LoadInit = function(){
            $.ajax({
                url: '@webAPI.CurrentValue.APIEmpHub/api/MasterCode/DataList'
                , headers: { 'authorization': 'Bearer @token' }
                , type: 'post'
                , contentType: 'application/json'
                , dataType: 'json'
                , cache: false
                , data: JSON.stringify({
                    groupName: 'departmentCode'
                })
                , success: function (result) {
                    $timeout(function () {
                        $scope.lSectionAll = result.filter(item => item.condition_4 == 'Section');
                        $scope.lDepartmentAll = result.filter(item => item.condition_4 == 'Department');
                        $scope.lGroupAll = result.filter(item => item.condition_4 == 'Group');
                        $scope.lBusinessUnitAll = result.filter(item => item.condition_4 == 'Business Unit');
                        $scope.BuildOrgOption();
                    }, 0);
                }
                , error: AjaxError
            });
            $.ajax({
                url: '@webAPI.CurrentValue.APIEmpHub/api/MasterCode/DataList'
                , headers: { 'authorization': 'Bearer @token' }
                , type: 'post'
                , contentType: 'application/json'
                , dataType: 'json'
                , cache: false
                , data: JSON.stringify({
                    groupName: 'positionCode'
                })
                , success: function (result) {
                    $timeout(function () {
                        $scope.lPosition = result;
                    }, 0);
                }
                , error: AjaxError
            });
            $.ajax({
                url: '@webAPI.CurrentValue.APIEmpHub/api/MasterCode/DataList'
                , headers: { 'authorization': 'Bearer @token' }
                , type: 'post'
                , contentType: 'application/json'
                , dataType: 'json'
                , cache: false
                , data: JSON.stringify({
                    groupName: 'levelCode'
                })
                , success: function (result) {
                    $timeout(function () {
                        $scope.lLevel = result;
                    }, 0);
                }
                , error: AjaxError
            });
            $.ajax({
                url: '@webAPI.CurrentValue.APIEmpHub/api/MasterCode/DataList'
                , headers: { 'authorization': 'Bearer @token' }
                , type: 'post'
                , contentType: 'application/json'
                , dataType: 'json'
                , cache: false
                , data: JSON.stringify({
                    groupName: 'Disability'
                })
                , success: function (result) {
                    $timeout(function () {
                        $scope.lDisability = result;
                    }, 0);
                }
                , error: AjaxError
            });
            $.ajax({
                url: '@webAPI.CurrentValue.APIEmpHub/api/MasterCode/DataList'
                , headers: { 'authorization': 'Bearer @token' }
                , type: 'post'
                , contentType: 'application/json'
                , dataType: 'json'
                , cache: false
                , data: JSON.stringify({
                    groupName: 'Ethnicity'
                })
                , success: function (result) {
                    $timeout(function () {
                        $scope.lEthnicity = result;
                    }, 0);
                }
                , error: AjaxError
            });
            $.ajax({
                url: '@webAPI.CurrentValue.APIEmpHub/api/MasterCode/DataList'
                , headers: { 'authorization': 'Bearer @token' }
                , type: 'post'
                , contentType: 'application/json'
                , dataType: 'json'
                , cache: false
                , data: JSON.stringify({
                    groupName: 'Marital'
                })
                , success: function (result) {
                    $timeout(function () {
                        $scope.lMarital = result;
                    }, 0);
                }
                , error: AjaxError
            });
            $.ajax({
                url: '@webAPI.CurrentValue.APIEmpHub/api/MasterCode/DataList'
                , headers: { 'authorization': 'Bearer @token' }
                , type: 'post'
                , contentType: 'application/json'
                , dataType: 'json'
                , cache: false
                , data: JSON.stringify({
                    groupName: 'Military'
                })
                , success: function (result) {
                    $timeout(function () {
                        $scope.lMilitary = result;
                    }, 0);
                }
                , error: AjaxError
            });
            $.ajax({
                url: '@webAPI.CurrentValue.APIEmpHub/api/MasterCode/DataList'
                , headers: { 'authorization': 'Bearer @token' }
                , type: 'post'
                , contentType: 'application/json'
                , dataType: 'json'
                , cache: false
                , data: JSON.stringify({
                    groupName: 'Nationality'
                })
                , success: function (result) {
                    $timeout(function () {
                        $scope.lNationality = MasterOptionListWithOther(result);
                        $scope.SyncNationality();
                        $scope.SyncPrefix();
                    }, 0);
                }
                , error: AjaxError
            });
            $.ajax({
                url: '@webAPI.CurrentValue.APIEmpHub/api/MasterCode/DataList'
                , headers: { 'authorization': 'Bearer @token' }
                , type: 'post'
                , contentType: 'application/json'
                , dataType: 'json'
                , cache: false
                , data: JSON.stringify({
                    groupName: 'Religion'
                })
                , success: function (result) {
                    $timeout(function () {
                        $scope.lReligion = result;
                    }, 0);
                }
                , error: AjaxError
            });
            $.ajax({
                url: '@webAPI.CurrentValue.APIEmpHub/api/MasterCode/DataList'
                , headers: { 'authorization': 'Bearer @token' }
                , type: 'post'
                , contentType: 'application/json'
                , dataType: 'json'
                , cache: false
                , data: JSON.stringify({
                    groupName: 'Prefix'
                })
                , success: function (result) {
                    $timeout(function () {
                        $scope.lPrefix = MasterOptionListWithOther(result);
                        $scope.SyncPrefix();
                    }, 0);
                }
                , error: AjaxError
            });
            $.ajax({
                url: '@webAPI.CurrentValue.APIEmpHub/api/MasterCode/DataList'
                , headers: { 'authorization': 'Bearer @token' }
                , type: 'post'
                , contentType: 'application/json'
                , dataType: 'json'
                , cache: false
                , data: JSON.stringify({
                    groupName: 'WorkMode'
                })
                , success: function (result) {
                    $timeout(function () {
                        $scope.lWorkMode = result;
                    }, 0);
                }
                , error: AjaxError
            });
            $.ajax({
                url: '@webAPI.CurrentValue.APIEmpHub/api/MasterCode/DataList'
                , headers: { 'authorization': 'Bearer @token' }
                , type: 'post'
                , contentType: 'application/json'
                , dataType: 'json'
                , cache: false
                , data: JSON.stringify({
                    groupName: 'OTMode'
                })
                , success: function (result) {
                    $timeout(function () {
                        $scope.lOTMode = result;
                    }, 0);
                }
                , error: AjaxError
            });
            $.ajax({
                url: '@webAPI.CurrentValue.APIEmpHub/api/MasterCode/Address'
                , headers: { 'authorization': 'Bearer @token' }
                , type: 'post'
                , dataType: 'json'
                , cache: false
                , success: function (result) {
                    $timeout(function () {
                        $scope.lAddress = result;
                    }, 0);
                }
                , error: AjaxError
            });
            $.ajax({
                url: '@webAPI.CurrentValue.APIEmpHub/api/MasterCode/DataList'
                , headers: { 'authorization': 'Bearer @token' }
                , type: 'post'
                , contentType: 'application/json'
                , dataType: 'json'
                , cache: false
                , data: JSON.stringify({
                    groupName: 'employeeType'
                })
                , success: function (result) {
                    $timeout(function () {
                        $scope.lEmployeeType = result;
                    }, 0);
                }
                , error: AjaxError
            });
        }
        $scope.LoadInit();

        $scope.MASTER_OTHER = MASTER_OTHER;

        // ถ้าค่าที่โหลดมาเป็นชื่อที่พิมพ์เอง ให้ตั้ง dropdown เป็น "อื่น ๆ" แล้วย้ายชื่อไปช่องกรอก
        $scope.SyncNationality = function () {
            if (!$scope.lNationality || !$scope.iPersonal) {
                return;
            }

            if (MasterIsOther($scope.lNationality, $scope.iPersonal.nationality, 'code')) {
                $scope.nationality_other = $scope.iPersonal.nationality;
                $scope.iPersonal.nationality = MASTER_OTHER;
            }
        }

        // เลขบัตรผู้พิการ: บังคับกรอกเฉพาะสถานะที่ตั้ง flag ไว้ที่หน้า Setting > Disability


        $scope.NeedDisabilityCard = function () {


            return IsDisabilityCardRequired($scope.lDisability, $scope.iPersonal.disabilityStatus);


        }


        // คำนำหน้าใน Onboarding เก็บเป็น code ทั้ง TH/EN
        $scope.SyncPrefix = function () {
            if (!$scope.lPrefix || !$scope.iPersonal) {
                return;
            }

            if (MasterIsOther($scope.lPrefix, $scope.iPersonal.prefix_th, 'code')) {
                $scope.prefix_th_other = $scope.iPersonal.prefix_th;
                $scope.iPersonal.prefix_th = MASTER_OTHER;
            }

            if (MasterIsOther($scope.lPrefix, $scope.iPersonal.prefix_en, 'code')) {
                $scope.prefix_en_other = $scope.iPersonal.prefix_en;
                $scope.iPersonal.prefix_en = MASTER_OTHER;
            }
        }

        $scope.SelectPrefixTH = function () {
            $scope.iPersonal.prefix_en = $scope.iPersonal.prefix_th;
        }

        $scope.SelectPrefixEN = function () {
            $scope.iPersonal.prefix_th = $scope.iPersonal.prefix_en;
        }

        $scope.SelectPosition = function () {
            var find = $scope.lPosition.find(item => item.code == $scope.iEmployee.positionCode);

            if (find) {
                $scope.iEmployee.grade = find.condition_2;
            }
        }

        // โครงสร้างหน่วยงาน: condition_4 = ระดับ, condition_3 = รหัสหน่วยงานแม่
        // Business Unit = ด้าน, Group = สาย, Department = ฝ่าย, Section = ส่วน
        $scope.lFunction = [];
        $scope.lDivision = [];
        $scope.lDepartment = [];
        $scope.lSection = [];

        function OrgChildren(lTier, parentCode) {
            if (!parentCode) {
                return [];
            }

            return (lTier || []).filter(function (x) { return x.condition_3 == parentCode; });
        }

        function FindOrgItem(lTier, code) {
            return (lTier || []).find(function (x) { return x.code == code; });
        }

        function IsOrgMasterReady() {
            return $scope.lBusinessUnitAll && $scope.lGroupAll && $scope.lDepartmentAll && $scope.lSectionAll;
        }

        function ResolveLegacyOrgCode() {
            if (!$scope.iEmployee || !IsOrgMasterReady() || !$scope.iEmployee.departmentCode || $scope.iEmployee.sectionCode) {
                return;
            }

            if (FindOrgItem($scope.lBusinessUnitAll, $scope.iEmployee.departmentCode)) {
                $scope.iEmployee.functionCode = $scope.iEmployee.functionCode || $scope.iEmployee.departmentCode;
                $scope.iEmployee.departmentCode = '';
                return;
            }

            if (FindOrgItem($scope.lGroupAll, $scope.iEmployee.departmentCode)) {
                $scope.iEmployee.divisionCode = $scope.iEmployee.divisionCode || $scope.iEmployee.departmentCode;
                $scope.iEmployee.departmentCode = '';
                return;
            }

            if (FindOrgItem($scope.lSectionAll, $scope.iEmployee.departmentCode)) {
                $scope.iEmployee.sectionCode = $scope.iEmployee.departmentCode;
                $scope.iEmployee.departmentCode = '';
            }
        }

        function ResolveOrgParentChain() {
            if (!$scope.iEmployee) {
                return;
            }

            if ($scope.iEmployee.sectionCode && !$scope.iEmployee.departmentCode) {
                var section = FindOrgItem($scope.lSectionAll, $scope.iEmployee.sectionCode);
                $scope.iEmployee.departmentCode = section ? section.condition_3 : '';
            }
            if ($scope.iEmployee.departmentCode && !$scope.iEmployee.divisionCode) {
                var department = FindOrgItem($scope.lDepartmentAll, $scope.iEmployee.departmentCode);
                $scope.iEmployee.divisionCode = department ? department.condition_3 : '';
            }
            if ($scope.iEmployee.divisionCode && !$scope.iEmployee.functionCode) {
                var division = FindOrgItem($scope.lGroupAll, $scope.iEmployee.divisionCode);
                $scope.iEmployee.functionCode = division ? division.condition_3 : '';
            }
        }

        function NormalizeOrgSelection() {
            if (!$scope.iEmployee) {
                return;
            }
            if (!IsOrgMasterReady()) {
                return;
            }

            if ($scope.iEmployee.functionCode && !FindOrgItem($scope.lBusinessUnitAll, $scope.iEmployee.functionCode)) {
                $scope.iEmployee.functionCode = '';
                $scope.iEmployee.divisionCode = '';
                $scope.iEmployee.departmentCode = '';
                $scope.iEmployee.sectionCode = '';
                return;
            }

            if ($scope.iEmployee.divisionCode) {
                var division = FindOrgItem($scope.lGroupAll, $scope.iEmployee.divisionCode);
                if (!division || division.condition_3 != $scope.iEmployee.functionCode) {
                    $scope.iEmployee.divisionCode = '';
                    $scope.iEmployee.departmentCode = '';
                    $scope.iEmployee.sectionCode = '';
                }
            }

            if ($scope.iEmployee.departmentCode) {
                var department = FindOrgItem($scope.lDepartmentAll, $scope.iEmployee.departmentCode);
                if (!department || department.condition_3 != $scope.iEmployee.divisionCode) {
                    $scope.iEmployee.departmentCode = '';
                    $scope.iEmployee.sectionCode = '';
                }
            }

            if ($scope.iEmployee.sectionCode) {
                var section = FindOrgItem($scope.lSectionAll, $scope.iEmployee.sectionCode);
                if (!section || section.condition_3 != $scope.iEmployee.departmentCode) {
                    $scope.iEmployee.sectionCode = '';
                }
            }
        }

        $scope.BuildOrgOption = function () {
            ResolveLegacyOrgCode();
            ResolveOrgParentChain();
            NormalizeOrgSelection();
            $scope.lFunction = $scope.lBusinessUnitAll || [];
            $scope.lDivision = OrgChildren($scope.lGroupAll, $scope.iEmployee.functionCode);
            $scope.lDepartment = OrgChildren($scope.lDepartmentAll, $scope.iEmployee.divisionCode);
            $scope.lSection = OrgChildren($scope.lSectionAll, $scope.iEmployee.departmentCode);
        }

        $scope.SelectFunction = function () {
            $scope.iEmployee.divisionCode = '';
            $scope.iEmployee.departmentCode = '';
            $scope.iEmployee.sectionCode = '';
            $scope.BuildOrgOption();
        }

        $scope.SelectDivision = function () {
            $scope.iEmployee.departmentCode = '';
            $scope.iEmployee.sectionCode = '';
            $scope.BuildOrgOption();
        }

        $scope.SelectDepartment = function () {
            $scope.iEmployee.sectionCode = '';
            $scope.BuildOrgOption();
        }

        $scope.LoadData = function () {
            $.ajax({
                url: '@webAPI.CurrentValue.APIEmpHub/api/Onboard/Detail'
                , headers: { 'authorization': 'Bearer @token' }
                , type: 'post'
                , contentType: 'application/json'
                , dataType: 'json'
                , cache: false
                , data: JSON.stringify({
                    userId: $scope.userId
                })
                , success: function (result) {
                    $timeout(function () {
                        $scope.iData = result;

                        if ($scope.iData.status != 'Draft') {
                            location.href = '@Url.Page("Index")';
                        }
                    }, 0);
                }
                , error: function () {
                    location.href = '@Url.Page("Index")';
                }
            });

            $.ajax({
                url: '@webAPI.CurrentValue.APIEmpHub/api/User/UserPersonal'
                , headers: { 'authorization': 'Bearer @token' }
                , type: 'post'
                , contentType: 'application/json'
                , dataType: 'json'
                , cache: false
                , data: JSON.stringify({
                    userId: $scope.userId
                    , create_by: '@User.UserId()'
                })
                , success: function (result) {
                    $timeout(function () {
                        $scope.iPersonal = result.data || {};

                        if ($scope.iPersonal.birth_date) {
                            $scope.iPersonal.birth_date_select = moment($scope.iPersonal.birth_date, "DD/MM/YYYY");
                        }

                        $scope.SyncNationality();
                        $scope.SyncPrefix();
                        $scope.CalcAge();
                    }, 0);
                }
                , error: AjaxError
            });
            
            $.ajax({
                url: '@webAPI.CurrentValue.APIEmpHub/api/User/UserAddress'
                , headers: { 'authorization': 'Bearer @token' }
                , type: 'post'
                , contentType: 'application/json'
                , dataType: 'json'
                , cache: false
                , data: JSON.stringify({
                    userId: $scope.userId
                    , create_by: '@User.UserId()'
                })
                , success: function (result) {
                    $timeout(function () {
                        $scope.iAddress = result.data || {};
                        $scope.SyncAddressSameFlags();
                    }, 0);
                }
                , error: AjaxError
            });

            $.ajax({
                url: '@webAPI.CurrentValue.APIEmpHub/api/User/UserEmployee'
                , headers: { 'authorization': 'Bearer @token' }
                , type: 'post'
                , contentType: 'application/json'
                , dataType: 'json'
                , cache: false
                , data: JSON.stringify({
                    userId: $scope.userId
                    , create_by: '@User.UserId()'
                })
                , success: function (result) {
                    $timeout(function () {
                        $scope.iEmployee = result.data || {};

                        if ($scope.iEmployee.join_date) {
                            $scope.iEmployee.join_date_select = moment($scope.iEmployee.join_date, "DD/MM/YYYY");
                        }
                        if ($scope.iEmployee.probation_end_date) {
                            $scope.iEmployee.probation_end_date_select = moment($scope.iEmployee.probation_end_date, "DD/MM/YYYY");
                        }
                        if (!$scope.iEmployee.probation_day && $scope.iEmployee.join_date_select && $scope.iEmployee.probation_end_date_select) {
                            $scope.calcProbationDay();
                        } else if ($scope.iEmployee.probation_day && $scope.iEmployee.join_date_select && !$scope.iEmployee.probation_end_date_select) {
                            $scope.calcProbationDate();
                        }
                        if ($scope.iEmployee.workMode && !$scope.iEmployee.workTime && $scope.lWorkMode) {
                            $scope.SelectWorkMode();
                        }
                        $scope.BuildOrgOption();
                    }, 0);
                }
                , error: AjaxError
            });
        }
        $scope.LoadData();

        $scope.ShowAddress = function (type) {
            $scope.address_type = type;
            $scope.filter_name = '';
            $("#addressModal").modal('show');
        }

        $scope.SelectAddress = function (x) {
            $timeout(function () {
                if ($scope.address_type == 'registered') {
                    $scope.iAddress.registered_subDistrictCode = x.subDistrictCode;
                    $scope.iAddress.registered_subDistrictName = x.subDistrictName;
                    $scope.iAddress.registered_districtCode = x.districtCode;
                    $scope.iAddress.registered_districtName = x.districtName;
                    $scope.iAddress.registered_provinceCode = x.provinceCode;
                    $scope.iAddress.registered_provinceName = x.provinceName;
                    $scope.iAddress.registered_postcode = x.postcode;
                } else if ($scope.address_type == 'card') {
                    $scope.iAddress.card_subDistrictCode = x.subDistrictCode;
                    $scope.iAddress.card_subDistrictName = x.subDistrictName;
                    $scope.iAddress.card_districtCode = x.districtCode;
                    $scope.iAddress.card_districtName = x.districtName;
                    $scope.iAddress.card_provinceCode = x.provinceCode;
                    $scope.iAddress.card_provinceName = x.provinceName;
                    $scope.iAddress.card_postcode = x.postcode;
                } else if ($scope.address_type == 'live') {
                    $scope.iAddress.live_subDistrictCode = x.subDistrictCode;
                    $scope.iAddress.live_subDistrictName = x.subDistrictName;
                    $scope.iAddress.live_districtCode = x.districtCode;
                    $scope.iAddress.live_districtName = x.districtName;
                    $scope.iAddress.live_provinceCode = x.provinceCode;
                    $scope.iAddress.live_provinceName = x.provinceName;
                    $scope.iAddress.live_postcode = x.postcode;
                }
                $("#addressModal").modal('hide');
            }, 0);
        }

        function AddressValue(name) {
            return ($scope.iAddress && $scope.iAddress[name]) || '';
        }

        function IsSameAsRegistered(prefix) {
            var fields = ['home', 'road', 'subDistrictCode', 'subDistrictName', 'districtCode', 'districtName', 'provinceCode', 'provinceName', 'postcode'];
            var hasRegisteredAddress = fields.some(function (field) {
                return !IsBlank(AddressValue('registered_' + field));
            });

            return hasRegisteredAddress && fields.every(function (field) {
                return AddressValue(prefix + '_' + field) == AddressValue('registered_' + field);
            });
        }

        $scope.SyncAddressSameFlags = function () {
            $scope.is_card_same_register = IsSameAsRegistered('card');
            $scope.is_live_same_register = IsSameAsRegistered('live');
        }

        function GetPickerDateValue(value) {
            if (!value) {
                return null;
            }
            if (moment.isMoment(value)) {
                return value.isValid() ? value.toDate() : null;
            }
            if (value instanceof Date) {
                return isNaN(value.getTime()) ? null : value;
            }
            if (typeof value === 'string') {
                var text = value.trim();

                if (!text) {
                    return null;
                }

                var parsed = moment(text, "DD/MM/YYYY", true);
                if (parsed.isValid()) {
                    return parsed.toDate();
                }

                var fallback = new Date(text);
                return isNaN(fallback.getTime()) ? null : fallback;
            }

            var parsedValue = moment(value);
            return parsedValue.isValid() ? parsedValue.toDate() : null;
        }

        function FormatPickerDate(value) {
            var date = GetPickerDateValue(value);
            return date ? moment(date).format("DD/MM/YYYY") : '';
        }

        function GetBirthDateValue() {
            return GetPickerDateValue($scope.iPersonal.birth_date_select);
        }

        $scope.CalcAge = function(){
            $scope.iPersonal.age = '';

            var b = GetBirthDateValue();

            if (b) {
                const c = new Date();
                b.setHours(0, 0, 0, 0);
                c.setHours(0, 0, 0, 0);

                if (b > c) {
                    return;
                }

                let years = c.getFullYear() - b.getFullYear();
                let months = c.getMonth() - b.getMonth();
                let days = c.getDate() - b.getDate();

                // ถ้าวันติดลบ → ยืมเดือน
                if (days < 0) {
                    months--;

                    // เอาจำนวนวันของเดือนก่อนหน้า
                    const prevMonth = new Date(c.getFullYear(), c.getMonth(), 0);
                    days += prevMonth.getDate();
                }

                // ถ้าเดือนติดลบ → ยืมปี
                if (months < 0) {
                    years--;
                    months += 12;
                }

                if (years > 0) {
                    $scope.iPersonal.age = years + ' ปี ';
                }
                if (months > 0) {
                    $scope.iPersonal.age += months + ' เดือน ';
                }
                if (days > 0) {
                    $scope.iPersonal.age += days + ' วัน';
                }
                if ($scope.iPersonal.age == '') {
                    $scope.iPersonal.age = '0 วัน';
                }
            }
        }

        $scope.$watch('iPersonal.birth_date_select', function (newValue, oldValue) {
            if (newValue !== oldValue) {
                $scope.CalcAge();
            }
        });

        $scope.calcProbationDay = function(){
            $scope.iEmployee.probation_day = 0;

            var startStr = FormatPickerDate($scope.iEmployee.join_date_select);
            var endStr = FormatPickerDate($scope.iEmployee.probation_end_date_select);

            if (startStr && endStr) {
                const start = ConvertToDate(startStr);
                const end = ConvertToDate(endStr);
                const diffTime = end - start;

                $scope.iEmployee.probation_day = parseInt(diffTime / (1000 * 60 * 60 * 24)) + 1;
            }
        }

        // เลือก/พิมพ์จำนวนวัน (หรือเปลี่ยนวันเข้างาน) แล้วคำนวณวันผ่านทดลองงานให้
        // บวก day - 1 เพราะ calcProbationDay นับวันเข้างานเป็นวันที่ 1 ค่าจึงกลับไปกลับมาได้ตรงกัน
        $scope.calcProbationDate = function () {
            var day = parseInt($scope.iEmployee.probation_day);

            var startStr = FormatPickerDate($scope.iEmployee.join_date_select);

            if (!startStr || !day || day < 1) {
                return;
            }

            var start = ConvertToDate(startStr);

            $scope.iEmployee.probation_end_date_select = new moment(start.addDays(day - 1));
        }

        $scope.SelectWorkMode = function(){
            $scope.iEmployee.workTime = '';

            if ($scope.iEmployee.workMode) {
                var find = $scope.lWorkMode.find(item => item.code == $scope.iEmployee.workMode);
                if(find){
                    $scope.iEmployee.workTime = find.condition_1 + '-' + find.condition_2;
                }
            }
        }

        $scope.$watchGroup(['iEmployee.workMode', 'lWorkMode'], function () {
            if ($scope.iEmployee && $scope.iEmployee.workMode && !$scope.iEmployee.workTime && $scope.lWorkMode) {
                $scope.SelectWorkMode();
            }
        });

        $scope.ShowSupervisor = function () {
            $scope.filter_name = '';
            if (!$scope.lEmployee) {
                $.ajax({
                    url: '@webAPI.CurrentValue.APIEmpHub/api/User/DataList'
                    , headers: { 'authorization': 'Bearer @token' }
                    , type: 'post'
                    , contentType: 'application/json'
                    , dataType: 'json'
                    , cache: false
                    , data: JSON.stringify({
                        page: 1
                        , row: 999
                    })
                    , success: function (result) {
                        $timeout(function () {
                            $scope.lEmployee = result.data;
                            $("#supervisorModal").modal('show');
                        }, 0);
                    }
                    , error: AjaxError
                });
            } else {
                $("#supervisorModal").modal('show');
            }
        }

        $scope.SelectSupervisor = function (x) {
            $("#supervisorModal").modal('hide');
            $scope.iEmployee.supervisorId = x.userId;
            $scope.iEmployee.supervisorName = x.firstname_th + ' ' + x.lastname_th;
        }

        $scope.SelectCardAddressUserRegistered = function () {
            $scope.iAddress.card_home = $scope.is_card_same_register ? $scope.iAddress.registered_home : '';
            $scope.iAddress.card_road = $scope.is_card_same_register ? $scope.iAddress.registered_road : '';
            $scope.iAddress.card_subDistrictCode = $scope.is_card_same_register ? $scope.iAddress.registered_subDistrictCode : '';
            $scope.iAddress.card_subDistrictName = $scope.is_card_same_register ? $scope.iAddress.registered_subDistrictName : '';
            $scope.iAddress.card_districtCode = $scope.is_card_same_register ? $scope.iAddress.registered_districtCode : '';
            $scope.iAddress.card_districtName = $scope.is_card_same_register ? $scope.iAddress.registered_districtName : '';
            $scope.iAddress.card_provinceCode = $scope.is_card_same_register ? $scope.iAddress.registered_provinceCode : '';
            $scope.iAddress.card_provinceName = $scope.is_card_same_register ? $scope.iAddress.registered_provinceName : '';
            $scope.iAddress.card_postcode = $scope.is_card_same_register ? $scope.iAddress.registered_postcode : '';
        }

        $scope.SelectLiveAddressUserRegistered = function () {
            $scope.iAddress.live_home = $scope.is_live_same_register ? $scope.iAddress.registered_home : '';
            $scope.iAddress.live_road = $scope.is_live_same_register ? $scope.iAddress.registered_road : '';
            $scope.iAddress.live_subDistrictCode = $scope.is_live_same_register ? $scope.iAddress.registered_subDistrictCode : '';
            $scope.iAddress.live_subDistrictName = $scope.is_live_same_register ? $scope.iAddress.registered_subDistrictName : '';
            $scope.iAddress.live_districtCode = $scope.is_live_same_register ? $scope.iAddress.registered_districtCode : '';
            $scope.iAddress.live_districtName = $scope.is_live_same_register ? $scope.iAddress.registered_districtName : '';
            $scope.iAddress.live_provinceCode = $scope.is_live_same_register ? $scope.iAddress.registered_provinceCode : '';
            $scope.iAddress.live_provinceName = $scope.is_live_same_register ? $scope.iAddress.registered_provinceName : '';
            $scope.iAddress.live_postcode = $scope.is_live_same_register ? $scope.iAddress.registered_postcode : '';
        }

        $scope.SavePersonal = function () {
            $scope.iPersonal.birth_date = FormatPickerDate($scope.iPersonal.birth_date_select);

            $scope.iPersonal.create_by = '@User.UserId()';
            $scope.iPersonal.userId = $scope.userId;

            // แทนรหัส OTHER ด้วยชื่อที่พิมพ์เอง โดยไม่แก้ $scope.iPersonal เพื่อไม่ให้ dropdown เพี้ยนหลังบันทึก
            var iSave = angular.extend({}, $scope.iPersonal);

            if (iSave.nationality == MASTER_OTHER) {
                iSave.nationality = ($scope.nationality_other || '').trim();
            }
            if (iSave.prefix_th == MASTER_OTHER) {
                iSave.prefix_th = ($scope.prefix_th_other || '').trim();
            }
            if (iSave.prefix_en == MASTER_OTHER) {
                iSave.prefix_en = ($scope.prefix_en_other || '').trim();
            }

            // เก็บเลขบัตรได้แม้ master ไม่ได้ตั้งเป็น required; ล้างเฉพาะกรณียังไม่เลือกสถานะความพิการ
            if (!iSave.disabilityStatus) {
                iSave.disabilityCardNo = '';
            }

            return new Promise((resolve) => {
                $.ajax({
                    url: '@webAPI.CurrentValue.APIEmpHub/api/User/UserPersonalSave'
                    , headers: { 'authorization': 'Bearer @token' }
                    , type: 'post'
                    , contentType: 'application/json'
                    , cache: false
                    , data: JSON.stringify(iSave)
                    , success: function () {
                        $timeout(function () {
                            resolve(true);
                        }, 0);
                    }
                    , error: function () {
                        alertWarning("Save Data Personal", "Error");
                        resolve(false);
                    }
                });
            });
        }

        $scope.SaveEmployee = function () {
            $scope.BuildOrgOption();

            $scope.iEmployee.join_date = FormatPickerDate($scope.iEmployee.join_date_select);
            $scope.iEmployee.probation_end_date = FormatPickerDate($scope.iEmployee.probation_end_date_select);

            $scope.iEmployee.create_by = '@User.UserId()';
            $scope.iEmployee.userId = $scope.userId;

            return new Promise((resolve) => {
                $.ajax({
                    url: '@webAPI.CurrentValue.APIEmpHub/api/User/UserEmployeeSave'
                    , headers: { 'authorization': 'Bearer @token' }
                    , type: 'post'
                    , contentType: 'application/json'
                    , cache: false
                    , data: JSON.stringify($scope.iEmployee)
                    , success: function () {
                        $timeout(function () {
                            resolve(true);
                        }, 0);
                    }
                    , error: function (jqXHR) {
                        alertWarning("Save Data Employee", jqXHR.responseText || "Error");
                        resolve(false);
                    }
                });
            });
        }

        $scope.SaveAddress = function () {

            $scope.iAddress.create_by = '@User.UserId()';
            $scope.iAddress.userId = $scope.userId;

            return new Promise((resolve) => {
                $.ajax({
                    url: '@webAPI.CurrentValue.APIEmpHub/api/User/UserAddressSave'
                    , headers: { 'authorization': 'Bearer @token' }
                    , type: 'post'
                    , contentType: 'application/json'
                    , cache: false
                    , data: JSON.stringify($scope.iAddress)
                    , success: function () {
                        $timeout(function () {
                            resolve(true);
                        }, 0);
                    }
                    , error: function () {
                        alertWarning("Save Data Address", "Error");
                        resolve(false);
                    }
                });
            });
        }

        $scope.SendOnboard = function () {
            return new Promise((resolve) => {
                $.ajax({
                    url: '@webAPI.CurrentValue.APIEmpHub/api/Onboard/Send'
                    , headers: { 'authorization': 'Bearer @token' }
                    , type: 'post'
                    , contentType: 'application/json'
                    , cache: false
                    , data: JSON.stringify({
                        userId: $scope.userId
                        , update_by: '@User.UserId()'
                    })
                    , success: function () {
                        $timeout(function () {
                            resolve(true);
                        }, 0);
                    }
                    , error: function () {
                        alertWarning("Send Onboard", "Error");
                        resolve(false);
                    }
                });
            });
        }


        function IsBlank(value) {
            return value == undefined || value == null || (typeof value === 'string' && value.trim() == '') || value == '';
        }

        function RequireField(messages, selector, label) {
            messages.push(label);
            JS_Require($(selector));
        }

        function ShowValidateMessages(stepIndex, messages) {
            if (messages.length == 0) {
                return true;
            }

            $scope.step_index = stepIndex;
            alertWarning("Form Validate", "กรุณาตรวจสอบ: " + messages.join(", "));
            return false;
        }

        $scope.CheckStep1 = function (options) {
            options = options || {};
            var messages = [];

            JS_Reset($(".nationality"));
            JS_Reset($(".nationality_other"));
            JS_Reset($(".idcard"));
            JS_Reset($(".prefix_th"));
            JS_Reset($(".prefix_th_other"));

            JS_Reset($(".disabilityCardNo"));
            JS_Reset($(".firstname_th"));
            JS_Reset($(".lastname_th"));
            JS_Reset($(".birth_date"));
            JS_Reset($(".sex"));
            JS_Reset($(".maritalStatus"));

            var birthDateValue = GetBirthDateValue();
            $scope.iPersonal.birth_date = birthDateValue ? moment(birthDateValue).format("DD/MM/YYYY") : '';

            if (IsBlank($scope.iPersonal.idcard)) {
                RequireField(messages, ".idcard", "ข้อมูลส่วนบุคคล > ข้อมูลยืนยันตัวตน > เลขประจำตัวประชาชน");
            }
            if (IsBlank($scope.iPersonal.nationality)) {
                RequireField(messages, ".nationality", "ข้อมูลส่วนบุคคล > ข้อมูลยืนยันตัวตน > สัญชาติ");
            }
            if ($scope.iPersonal.nationality == MASTER_OTHER && IsBlank($scope.nationality_other)) {
                RequireField(messages, ".nationality_other", "ข้อมูลส่วนบุคคล > ข้อมูลยืนยันตัวตน > ระบุสัญชาติ");
            }
            if (IsBlank($scope.iPersonal.prefix_th)) {
                RequireField(messages, ".prefix_th", "ข้อมูลส่วนบุคคล > ชื่อภาษาไทย > คำนำหน้าชื่อ");
            }
            if ($scope.iPersonal.prefix_th == MASTER_OTHER && IsBlank($scope.prefix_th_other)) {
                RequireField(messages, ".prefix_th_other", "ข้อมูลส่วนบุคคล > ชื่อภาษาไทย > ระบุคำนำหน้าชื่อ");
            }
            if ($scope.NeedDisabilityCard() && IsBlank($scope.iPersonal.disabilityCardNo)) {
                RequireField(messages, ".disabilityCardNo", "ข้อมูลส่วนบุคคล > ข้อมูลทั่วไป > เลขบัตรผู้พิการ");
            }
            if (IsBlank($scope.iPersonal.firstname_th)) {
                RequireField(messages, ".firstname_th", "ข้อมูลส่วนบุคคล > ชื่อภาษาไทย > ชื่อ");
            }
            if (IsBlank($scope.iPersonal.lastname_th)) {
                RequireField(messages, ".lastname_th", "ข้อมูลส่วนบุคคล > ชื่อภาษาไทย > นามสกุล");
            }
            if (IsBlank($scope.iPersonal.birth_date)) {
                RequireField(messages, ".birth_date", "ข้อมูลส่วนบุคคล > ข้อมูลทั่วไป > วันเกิด");
            }
            if (IsBlank($scope.iPersonal.sex)) {
                RequireField(messages, ".sex", "ข้อมูลส่วนบุคคล > ข้อมูลทั่วไป > เพศ");
            }
            if (IsBlank($scope.iPersonal.maritalStatus)) {
                RequireField(messages, ".maritalStatus", "ข้อมูลส่วนบุคคล > ข้อมูลทั่วไป > สถานภาพการสมรส");
            }

            if (messages.length == 0) {
                if (!options.skipMove) {
                $scope.step_index = 1;
                }
                return true;
            } else {
                return ShowValidateMessages(0, messages);
            }
        }

        $scope.CheckStep2 = function (options) {
            options = options || {};
            var messages = [];

            JS_Reset($(".registered_home"));
            JS_Reset($(".registered_provinceName"));
            JS_Reset($(".card_home"));
            JS_Reset($(".card_provinceName"));
            JS_Reset($(".live_home"));
            JS_Reset($(".live_provinceName"));


            if (IsBlank($scope.iAddress.registered_home)) {
                RequireField(messages, ".registered_home", "ข้อมูลที่อยู่ > ที่อยู่ทะเบียนบ้าน > บ้านเลขที่");
            }
            if (IsBlank($scope.iAddress.registered_provinceName)) {
                RequireField(messages, ".registered_provinceName", "ข้อมูลที่อยู่ > ที่อยู่ทะเบียนบ้าน > จังหวัด");
            }
            if (IsBlank($scope.iAddress.card_home)) {
                RequireField(messages, ".card_home", "ข้อมูลที่อยู่ > ที่อยู่ตามบัตรประชาชน > บ้านเลขที่");
            }
            if (IsBlank($scope.iAddress.card_provinceName)) {
                RequireField(messages, ".card_provinceName", "ข้อมูลที่อยู่ > ที่อยู่ตามบัตรประชาชน > จังหวัด");
            }
            if (IsBlank($scope.iAddress.live_home)) {
                RequireField(messages, ".live_home", "ข้อมูลที่อยู่ > ที่อยู่พักอาศัย > บ้านเลขที่");
            }
            if (IsBlank($scope.iAddress.live_provinceName)) {
                RequireField(messages, ".live_provinceName", "ข้อมูลที่อยู่ > ที่อยู่พักอาศัย > จังหวัด");
            }

            if (messages.length == 0) {
                if (!options.skipMove) {
                $scope.step_index = 2;
                }
                return true;
            } else {
                return ShowValidateMessages(1, messages);
            }
        }

        $scope.CheckStep3 = function (options) {
            options = options || {};
            var messages = [];

            
            JS_Reset($(".mobile"));
            JS_Reset($(".email_office"));

            if (IsBlank($scope.iPersonal.mobile)) {
                RequireField(messages, ".mobile", "ข้อมูลการติดต่อ > มือถือ");
            } else if (JS_ValidateMobile($scope.iPersonal.mobile) == false) {
                RequireField(messages, ".mobile", "ข้อมูลการติดต่อ > รูปแบบมือถือไม่ถูกต้อง");
            }
            if (!IsBlank($scope.iEmployee.email) && JS_ValidateEmail($scope.iEmployee.email) == false) {
                RequireField(messages, ".email_office", "ข้อมูลการติดต่อ > รูปแบบอีเมล์ที่ใช้ในบริษัทไม่ถูกต้อง");
            }


            if (messages.length == 0) {
                if (!options.skipMove) {
                $scope.step_index = 3;
                }
                return true;
            } else {
                return ShowValidateMessages(2, messages);
            }
        }

        $scope.CheckStep4 = function () {
            var messages = [];

            JS_Reset($(".employeeCode"));
            JS_Reset($(".join_date"));
            JS_Reset($(".functionCode"));
            JS_Reset($(".divisionCode"));
            JS_Reset($(".departmentCode"));
            JS_Reset($(".sectionCode"));
            JS_Reset($(".positionCode"));
            JS_Reset($(".levelCode"));
            JS_Reset($(".employeeType"));
            JS_Reset($(".employeeMode"));

            $scope.iEmployee.join_date = '';

            $scope.iEmployee.join_date = FormatPickerDate($scope.iEmployee.join_date_select);

            if (IsBlank($scope.iEmployee.employeeCode)) {
                RequireField(messages, ".employeeCode", "ข้อมูลพนักงาน > รหัสพนักงาน");
            }
            if (IsBlank($scope.iEmployee.join_date)) {
                RequireField(messages, ".join_date", "ข้อมูลพนักงาน > วันเข้างาน");
            }
            if (IsBlank($scope.iEmployee.functionCode)) {
                RequireField(messages, ".functionCode", "ข้อมูลพนักงาน > ด้าน");
            }
            if (IsBlank($scope.iEmployee.positionCode)) {
                RequireField(messages, ".positionCode", "ข้อมูลพนักงาน > ตำแหน่งงาน");
            }
            if (IsBlank($scope.iEmployee.employeeType)) {
                RequireField(messages, ".employeeType", "ข้อมูลพนักงาน > ประเภทพนักงาน");
            }
            if (IsBlank($scope.iEmployee.levelCode)) {
                RequireField(messages, ".levelCode", "ข้อมูลพนักงาน > ระดับพนักงาน");
            }
            // if ($scope.iEmployee.employeeMode == '') {
            //     flag = false;
            //     console.log('validate : employeeMode');
            //     JS_Require($(".employeeMode"));
            // }

            return ShowValidateMessages(3, messages);
        }

        
        //Action 
        
        $scope.Save = async function () {
            var flag = await $scope.SavePersonal();
            flag = await $scope.SaveEmployee();
            flag = await $scope.SaveAddress();

            if(flag){
                alertSuccess("Save Change", "Success");
            }
        }

        $scope.Send = async function () {
            if ($scope.CheckStep1({ skipMove: true }) && $scope.CheckStep2({ skipMove: true }) && $scope.CheckStep3({ skipMove: true }) && $scope.CheckStep4()) {
                var flag = await $scope.SavePersonal();
                flag = await $scope.SaveEmployee();
                flag = await $scope.SaveAddress();
                flag = await $scope.SendOnboard();

                if (flag) {
                    alertSuccess("Create Onboarding", "Success", function () {
                        location.href = '@Url.Page("Index")';
                    });
                }
            }
        }

    });
</script>
