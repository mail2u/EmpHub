@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<div>
    <div class="row">
        <div class="col-sm-4 mb-3">
            <div class="text-dark fw-bolder">วันเข้างาน</div>
            <div>{{ iEmployee.join_date == '' ? '-' : iEmployee.join_date }}</div>
        </div>
        <div class="col-sm-4 mb-3">
            <div class="text-dark fw-bolder">วันบรรจุงาน</div>
            <div>{{ iEmployee.probation_end_date == '' ? '-' : iEmployee.probation_end_date }}</div>
        </div>
        <div class="col-sm-4 mb-3">
            <div class="text-dark fw-bolder">สถานะ</div>
            <div>{{ iEmployee.employeeStatus == '' ? '-' : iEmployee.employeeStatus }}</div>
        </div>
        <div class="col-sm-4 mb-3">
            <div class="text-dark fw-bolder">อายุงาน</div>
            <div>{{ iEmployee.work_day == '' ? '-' : iEmployee.work_day }}</div>
        </div>
@*         <div class="col-sm-4 mb-3">
            <div class="text-dark fw-bolder">บริษัท</div>
            <div>{{ iEmployee.company == '' ? '-' : iEmployee.company }}</div>
        </div>
        <div class="col-sm-4 mb-3">
            <div class="text-dark fw-bolder">สถานที่</div>
            <div>{{ iEmployee.location == '' ? '-' : iEmployee.location }}</div>
        </div> *@
        <div class="col-sm-4 mb-3">
            <div class="text-dark fw-bolder">ด้าน</div>
            <div>{{ iEmployee.functionDesc == '' ? '-' : iEmployee.functionDesc }}</div>
        </div>
        <div class="col-sm-4 mb-3">
            <div class="text-dark fw-bolder">สาย</div>
            <div>{{ iEmployee.divisionDesc == '' ? '-' : iEmployee.divisionDesc }}</div>
        </div>
        <div class="col-sm-4 mb-3">
            <div class="text-dark fw-bolder">ฝ่าย</div>
            <div>{{ iEmployee.departmentDesc == '' ? '-' : iEmployee.departmentDesc }}</div>
        </div>
        <div class="col-sm-4 mb-3">
            <div class="text-dark fw-bolder">ส่วน</div>
            <div>{{ iEmployee.sectionDesc == '' ? '-' : iEmployee.sectionDesc }}</div>
        </div>
        <div class="col-sm-4 mb-3">
            <div class="text-dark fw-bolder">ตำแหน่งงาน</div>
            <div>{{ iEmployee.positionDesc == '' ? '-' : iEmployee.positionDesc }}</div>
        </div>
        <div class="col-sm-4 mb-3">
            <div class="text-dark fw-bolder">ประเภทพนักงาน</div>
            <div>{{ iEmployee.employeeType == '' ? '-' : iEmployee.employeeType }}</div>
        </div>
        <div class="col-sm-4 mb-3">
            <div class="text-dark fw-bolder">ระดับพนักงาน</div>
            <div>{{ iEmployee.levelDesc == '' ? '-' : iEmployee.levelDesc }}</div>
        </div>
        <div class="col-sm-4 mb-3">
            <div class="text-dark fw-bolder">เกรด</div>
            <div>{{ iEmployee.grade == '' ? '-' : iEmployee.grade }}</div>
        </div>
        <div class="col-sm-4 mb-3">
            <div class="text-dark fw-bolder">หัวหน้างาน</div>
            <div>{{ iEmployee.supervisorName == '' ? '-' : iEmployee.supervisorName }}</div>
        </div>
    </div>
    <hr class="my-3" />
    <div class="mb-2 d-flex align-items-center">
        <div class="flex-fill">
            <div>
                ปรับปรุงล่าสุด <span class="fw-bolder">{{ iEmployee.update_date }}</span> โดย <span class="fw-bolder">{{ iEmployee.update_by }}</span>
            </div>
        </div>
        <div>
            <a class="btn btn-sm btn-warning-invert" href="@Url.Page("/Service/FormCreate/FormUpdateDataEmployee")?mode=EDIT_EMPLOYEE&id={{userId}}" target="_blank">
                <i class="bi bi-pen me-2"></i> แก้ไข
            </a>
        </div>
    </div>
</div>